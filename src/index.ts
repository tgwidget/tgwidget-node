export { tgwidget, TgWidget } from "./builder";
export { parseDate, parseColor, parseSchedule } from "./parser";
export type {
  DateMode,
  DateFormat,
  DateOrder,
  ColorFormat,
  ColorScheme,
  WidgetStyle,
  DateResult,
  ColorResult,
  ScheduleDay,
} from "./types";
